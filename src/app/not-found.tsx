'use client'

import { Template } from "@/components";

export default function NotfoundPage(){
    return (
        <Template>
            <section>
                <h1 className="text-stone-950 text-xl text-center">Page not found!</h1>
            </section>
        </Template>
    )
}